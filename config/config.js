const config = {
    database: 'servers_db',
    username: 'root1',
     password: 'newpassword1',
     host: 'localhost',
     dialect: 'mysql',
  };
  // const environment = process.env.NODE_ENV || 'pre_prod';
  // const dbConfig = config[environment];
  
  module.exports = config;
  